spring:
  config:
    activate:
      on-profile: aws

  data:
    mongodb:
      uri: ${MONGODB_URI}
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

logging:
  level:
    com.adavis.iiot: INFO
    org.springframework.data.mongodb: INFO
